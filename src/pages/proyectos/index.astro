---
import BaseProyectLayout from '~/layouts/BaseProyectLayout.astro';
import { cardsContent } from '~/data/4-Proyects';
import { Image } from 'astro:assets';
---

<BaseProyectLayout
  title='Nuestros Proyectos | Ambientación Plantas Oficinas | Creamor Green'
  description='Descubre +100 proyectos de ambientación con plantas en oficinas y eventos. Casos reales de transformación de espacios corporativos en Santiago.'
  ogImage='/slide_image-1.jpg'
>
  <div class='container-width flex flex-col gap-10'>
    <div class='card-container'>
      <h1
        class='text-4xl font-bold border-l-8 border-[var(--borders)] pl-3 lg:mt-0'
      >
        Proyectos
      </h1>
      <div
        class='cards animation-card'
        data-aos='fade-up'
        data-aos-delay='400'
      >
        <div class='image-container'>
          <Image
            class='image-style'
            src='/slide_image-1.jpg'
            alt='slide_image-1'
            width={400}
            height={400}
          />
        </div>
        <div class='cards-content'>
          <h3 class='title-card'>{cardsContent[0].title}</h3>
          <p class='text-card'>
            {cardsContent[0].subtitle}
          </p>
          <div class='item-meta'>
            <p class='meta-item'>📍 {cardsContent[0].location}</p>
            <p class='meta-item'>🌱 {cardsContent[0].plants}+ Plantas</p>
            <p class='meta-item'>⏱️ {cardsContent[0].time} Semanas</p>
          </div>
          <a
            href='/proyectos/restaurante-verde-providencia'
            class='read-more'
            >Leer caso completo</a
          >
        </div>
      </div>
      <div
        class='cards animation-card'
        data-aos='fade-up'
        data-aos-delay='400'
      >
        <div class='image-container'>
          <Image
            class='image-style'
            src='/slide_image-1.jpg'
            alt='slide_image-1'
            width={400}
            height={400}
          />
        </div>
        <div class='cards-content'>
          <h3 class='title-card'>{cardsContent[0].title}</h3>
          <p class='text-card'>
            {cardsContent[0].subtitle}
          </p>
          <div class='item-meta'>
            <p class='meta-item'>📍 {cardsContent[0].location}</p>
            <p class='meta-item'>🌱 {cardsContent[0].plants}+ Plantas</p>
            <p class='meta-item'>⏱️ {cardsContent[0].time} Semanas</p>
          </div>
          <a
            href='#'
            class='read-more'
            >Leer caso completo</a
          >
        </div>
      </div>
    </div>
  </div>
</BaseProyectLayout>
<style>
  @reference 'tailwindcss';

  /* === LAYOUT PRINCIPAL === */
  .bg-proyects {
    @apply bg-[var(--background2)];
  }

  /* === INTRODUCCIÓN === */

  h1 {
    @apply text-balance;
  }

  /* === CONTENEDOR DE TARJETAS === */
  .card-container {
    @apply grid grid-cols-1 gap-10 justify-center items-center  p-8 pb-20 mt-[100px] lg:mt-[0];
    grid-template-rows: auto repeat(auto-fit, 400px);
  }

  /* === TARJETA PRINCIPAL === */
  .cards {
    @apply min-h-[400px] h-full bg-[var(--bg-cards)] p-4 lg:pl-0 grid grid-rows-[auto_1fr] grid-cols-[1fr] lg:grid-rows-[1fr] lg:grid-cols-[auto_1fr] gap-10;
    @apply w-full rounded-md;
  }

  /* === IMAGEN === */
  .image-container {
    @apply w-full h-full flex justify-center items-center;
  }

  .image-style {
    @apply transition-all ease-in-out duration-500;
  }

  .cards:hover .image-container .image-style {
    @apply brightness-75;
  }

  /* === CONTENIDO TEXTO === */
  .cards-content {
    @apply grid grid-rows-[auto_1fr_auto_auto] items-start lg:p-[45px] gap-4;
  }

  .title-card {
    @apply text-2xl font-bold;
  }

  .text-card {
    @apply line-clamp-5 overflow-hidden text-lg;
  }

  /* === METADATA === */
  .item-meta {
    @apply flex gap-2;
  }

  .meta-item {
    @apply text-sm;
  }

  /* === BOTÓN LEER MÁS === */
  .read-more {
    @apply inline-flex items-center gap-2 text-[#7fb069] font-medium transition-all duration-500 ease-in-out;
  }

  .read-more:hover {
    @apply text-[#5a8a4a] translate-x-[10px];
  }
</style>
